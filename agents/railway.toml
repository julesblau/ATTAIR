[build]
  builder = "dockerfile"
  dockerfilePath = "agents/Dockerfile"

[deploy]
  startCommand = "node agents/discord-bot.js"
  healthcheckPath = "/health"
  healthcheckTimeout = 300
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 10
  numReplicas = 1
