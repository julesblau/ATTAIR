You are a senior full-stack engineer building a feature for ATTAIRE (AI fashion shopping app).

REPO: C:\Users\jubla\OneDrive\Documents\ATTAIR
TECH: React 19 + Vite frontend (attair-app/src/App.jsx is the main file), Node/Express + Supabase backend (attair-backend/), Claude AI vision, SerpAPI.

TASK:
Follow-up Nudge System

Bot re-pings Jules after 10-15 min timeout if he misses a message that needs input.

JUDGE FEEDBACK (iteration 1):
**NEEDS_WORK**

The app is **completely broken** — all three screenshots show the same Vite compile error, meaning zero pages render:

```
[PARSE_ERROR] Error: Expected `,` or `)` but found `{`
── src/App.jsx:6950:23

6950 │ {/* Follow-up nudge toggle */}
     └── `,` or `)` expected
```

### Critical Issues:

1. **🔴 SYNTAX ERROR in `App.jsx` line 6950** — The follow-up nudge toggle JSX block has a parse error (likely a malformed JSX expression or missing closing parenthesis before the comment). The entire frontend fails to compile. No page loads. This is a ship-stopping bug.

2. **🔴 Frontend is 100% non-functional** — All three screenshots (home, scan, profile) display the identical Vite error overlay instead of any UI. Users see nothing but a red error screen.

### Fix Required:

- Open `src/App.jsx` around line 6948-6965 and fix the JSX syntax error in the follow-up nudge settings toggle. The `(` opened at line 6948 expects a `)` but instead encounters a JSX comment block `{/* ... */}`, suggesting a missing closing paren, misplaced fragment, or broken ternary/conditional rendering.

Backend tests pass (377/377 ✅), and the backend nudge architecture looks solid — but none of that matters when the frontend doesn't compile.

This is iteration 2. The previous build did NOT pass quality review. Fix the issues identified by the judge (see below) and bring this to production quality.

INSTRUCTIONS:
1. Read the relevant files first to understand current state.
2. Build the feature to FULL PRODUCTION quality — not a skeleton, not a placeholder. Ship-ready code.
3. Follow existing patterns in the codebase (design tokens in index.css, component styles in App.css).
4. The app should look and feel as polished as TikTok, Depop, Pickle, or Instagram.
5. After building, run the backend tests: cd attair-backend && npm test
6. Fix any test failures before finishing.
7. Commit your changes with: git add -A && git commit -m "feat: [short summary]"

Do NOT create new files unless absolutely necessary. Build within existing files.
Do NOT change function signatures in attair-backend/src/services/products.js.