You are a senior full-stack engineer building a feature for ATTAIRE (AI fashion shopping app).

REPO: C:\Users\jubla\OneDrive\Documents\ATTAIR
TECH: React 19 + Vite frontend (attair-app/src/App.jsx is the main file), Node/Express + Supabase backend (attair-backend/), Claude AI vision, SerpAPI.

TASK:
Style Twins — Find Your Fashion Doppelganger

Compute Euclidean distance between Style DNA scores. Surface 5-10 "Style Twins" on Discover tab. Weekly notification: "3 new style twins discovered." Shared saves trigger: "Your Style Twin also saved this."

JUDGE FEEDBACK (iteration 1):
**NEEDS_WORK**

The feature has solid backend architecture and good test coverage (459 passing), but there are specific issues that block shipping:

1. **Twins tab "cards" view is completely blank.** The `discover-twins-cards.png` screenshot shows the Twins tab selected but the content area is entirely empty — no featured card, no grid, no twin results rendered at all. The UI that was built (featured card, grid layout, match badges) isn't actually rendering in the screenshot. This is the core of the feature and it's not visible.

2. **Empty state shows no content.** The first "empty" twins screenshot (with Twins tab active) shows a completely blank screen with zero messaging — no "no twins found" copy, no loading indicator, nothing. Compare this to the second empty state screenshot which correctly shows "Unlock Style Twins" with a CTA. The blank-screen state is a broken UX path.

3. **`style_twins` notification preference not mapped in `prefMap`.** The key is listed as an allowed preference value (notifications.js line 160) but is missing from `prefMap` (lines 71-79). This means users **cannot disable** Style Twins notifications — a real bug that violates user trust.

4. **Weekly notify cron endpoint exists but is not wired.** `POST /api/style-twins/weekly-notify` has no cron job configuration. The "3 new style twins discovered" weekly notification will never fire. This is a stated requirement of the feature spec.

5. **The three screenshots submitted for judgment (home, scan, profile) don't show the Style Twins feature at all.** They show the landing page camera view. The feature's actual UI was only captured in earlier, separate screenshots — suggesting the screenshot pipeline isn't capturing the right screens for this feature's review.

**What's good:** Comparison sheet is polished (94% match ring, shared traits, palette, shared saves). Save toast banner looks clean. Empty "Unlock" state is well-designed. Backend algorithm is correct and well-tested. Rate limiting and auth are solid.

**To ship:** Fix the blank twins cards rendering, wire the notification preference toggle, configure the weekly cron, and capture screenshots that prove the populated twins grid actually renders.

JUDGE FEEDBACK (iteration 2):
**NEEDS_WORK**

Tests pass (459/459) ✅ and two of the previous bugs were fixed (prefMap now includes `style_twins`, weekly cron is wired in `index.js`). However:

1. **None of the three submitted screenshots show the Style Twins feature.** All three screenshots (home, scan, profile) show the identical ATTAIRE landing page with the camera/photo placeholder and "See it. Scan it. Shop it." hero. The Discover tab with the Twins sub-tab is not captured in any screenshot. I cannot verify the core UI of this feature — the twins cards grid, the match badges, the comparison sheet — actually renders. This was called out in iteration 1 and is **still not fixed**.

2. **The blank-screen race condition on the Twins tab persists.** When a user with Style DNA navigates to the Twins tab, there's a frame where `styleTwinsLoading === false` and `styleTwinsReady === false` (before the useEffect fires `loadStyleTwins()`). During that frame, users see the "Unlock Style Twins" CTA even though they already have Style DNA — a confusing/misleading state. The fix: set `styleTwinsLoading = true` synchronously when the tab activates, or trigger the load immediately on tab switch rather than relying on the useEffect dependency chain.

**To ship:**
- Capture screenshots that actually show the Twins tab with populated twin cards rendering (the entire point of this feature).
- Fix the initial-load race condition so authenticated users with Style DNA see a loading spinner, not the "Unlock" CTA, during the fetch gap.

JUDGE FEEDBACK (iteration 3):
**NEEDS_WORK**

This is iteration 3 and the same critical issue from iterations 1 and 2 remains unfixed:

1. **All three screenshots are identical and show ZERO Style Twins UI.** Every screenshot (home, scan, profile) shows the exact same ATTAIRE landing page with "Your outfit photo" camera placeholder and "See it. Scan it. Shop it." hero copy. Not a single screenshot captures the Discover tab, the Twins sub-tab, the twins cards grid, the match badges, or the comparison sheet. **This is the third consecutive iteration where the screenshot pipeline fails to capture the actual feature being judged.** I cannot approve a feature I cannot see rendering.

2. **The initial-load race condition (from iteration 2) was not addressed.** Users with Style DNA still see the "Unlock Style Twins" CTA momentarily before the fetch resolves, because `styleTwinsLoading` is not set to `true` synchronously on tab activation.

**To ship (same as last time, because nothing changed):**
- **Fix the screenshot pipeline** so it navigates to the Discover → Twins tab and captures: (a) the populated twins card grid with match percentages, (b) the comparison sheet for a twin, and (c) the empty/unlock state. Without visual proof the UI renders, this feature cannot be approved.
- **Fix the race condition** so authenticated users with Style DNA see a loading spinner (not the "Unlock" CTA) during the fetch gap when switching to the Twins tab.

Tests passing (459/459) and backend fixes from iteration 2 are acknowledged and appreciated — but the core deliverable of this feature is a UI that surfaces Style Twins to users, and I have zero evidence that UI actually renders.

JUDGE FEEDBACK (iteration 4):
**NEEDS_WORK**

This is **iteration 4** and the exact same blocking issue persists from iterations 1, 2, and 3:

1. **All three screenshots show ZERO Style Twins UI — for the fourth consecutive iteration.** Every screenshot is the identical ATTAIRE landing page with "Your outfit photo" camera placeholder and "See it. Scan it. Shop it." hero. Not one screenshot captures the Discover tab, the Twins sub-tab, the twins card grid, match badges, or comparison sheet. **I have never once seen visual proof that this feature's UI renders.** A feature I cannot see rendering cannot ship.

2. **The initial-load race condition (raised in iteration 2, unaddressed in iterations 3 and 4).** Users with Style DNA still momentarily see the "Unlock Style Twins" CTA before the fetch resolves, because `styleTwinsLoading` is not set to `true` synchronously on tab activation.

**What's acknowledged as good:** 459/459 tests passing, backend algorithm correct, `style_twins` preference mapped in `prefMap`, weekly cron wired in `index.js`.

**To ship (unchanged from the last three iterations):**
- **Fix the screenshot pipeline** so it navigates to Discover → Twins tab and captures: (a) populated twins card grid with match percentages, (b) a twin comparison sheet, (c) the empty/unlock state. This is non-negotiable — I need visual proof the core UI renders.
- **Fix the race condition** so authenticated users with Style DNA see a loading spinner, not the "Unlock" CTA, during the fetch gap on tab switch.

JUDGE FEEDBACK (iteration 5):
**NEEDS_WORK**

This is **iteration 5** and the identical blocking issues from iterations 1–4 remain:

1. **All three screenshots show ZERO Style Twins UI — for the fifth consecutive iteration.** Every screenshot is the same ATTAIRE landing page with "Your outfit photo" camera placeholder and "See it. Scan it. Shop it." hero. Not a single screenshot captures the Discover tab, the Twins sub-tab, the twins card grid, match badges, or comparison sheet. **I have asked for this in every single iteration. The screenshot pipeline is fundamentally broken for this feature — it never navigates to the Twins tab.** I cannot approve a feature whose core UI I have never seen render.

2. **The initial-load race condition (raised in iteration 2, unaddressed in iterations 3, 4, and 5).** Users with Style DNA still momentarily see the "Unlock Style Twins" CTA before the fetch resolves, because `styleTwinsLoading` is not set to `true` synchronously on tab activation.

**What's acknowledged as good:** 459/459 tests passing, backend algorithm correct, `style_twins` preference mapped, weekly cron wired.

**To ship (unchanged since iteration 2 — please actually do these):**
- **Fix the screenshot pipeline** so it navigates to the Discover → Twins tab and captures: (a) the populated twins card grid with match percentages, (b) a twin comparison sheet, (c) the empty/unlock state. This is the **entire point** of the feature review. Without visual proof the UI renders, approval is impossible.
- **Fix the race condition** so authenticated users with Style DNA see a loading spinner (not the "Unlock" CTA) during the fetch gap when switching to the Twins tab. Set `styleTwinsLoading = true` synchronously when the Twins tab activates, before the async fetch fires.

JUDGE FEEDBACK (iteration 6):
**NEEDS_WORK**

This is **iteration 6** and the same two blocking issues from iterations 1–5 remain completely unresolved:

1. **All three screenshots show ZERO Style Twins UI — for the sixth consecutive iteration.** Every screenshot is the identical ATTAIRE landing page with "Your outfit photo" camera placeholder and "See it. Scan it. Shop it." hero. Not a single screenshot captures the Discover tab, the Twins sub-tab, the twins card grid, match badges, or comparison sheet. **The screenshot pipeline is fundamentally broken — it has never once navigated to the Twins tab across six iterations.** I cannot approve a feature I have never seen render. This is non-negotiable.

2. **The initial-load race condition (raised in iteration 2, unaddressed in iterations 3–6).** Users with Style DNA still momentarily see the "Unlock Style Twins" CTA before the fetch resolves, because `styleTwinsLoading` is not set to `true` synchronously on tab activation.

**What's acknowledged as good:** 459/459 tests passing, backend algorithm correct, `style_twins` preference mapped in `prefMap`, weekly cron wired in `index.js`.

**To ship (same requirements for the sixth time):**
- **Fix the screenshot pipeline** so it navigates to Discover → Twins tab and captures: (a) the populated twins card grid with match percentages, (b) a twin comparison sheet, (c) the empty/unlock state. The screenshot script needs to be updated — it currently only captures home/scan/profile views and never touches the Discover tab. This is the **only** way I can verify the feature's core UI renders.
- **Fix the race condition** so authenticated users with Style DNA see a loading spinner (not the "Unlock" CTA) during the fetch gap when switching to the Twins tab. Set `styleTwinsLoading = true` synchronously when the Twins tab activates, before the async fetch fires.

JUDGE FEEDBACK (iteration 7):
Based on my thorough review of the code and screenshots, here is my judgment:

---

**NEEDS_WORK**

This is iteration 7 and I must be precise about what's actually broken vs. what's been fixed.

### ✅ FIXED (acknowledged):
- **Race condition: FIXED.** Line 5228 now shows the loading spinner when `authed && (styleTwinsLoading || !styleTwinsHasFetched)` — the `!styleTwinsHasFetched` guard ensures the "Unlock Style Twins" CTA **never** flashes before the initial fetch completes. The Unlock CTA (line 5261) requires `styleTwinsHasFetched === true`. This is a correct, complete fix.
- **`style_twins` in prefMap:** Fixed.
- **Weekly cron wired:** Fixed.
- **459/459 tests pass.**
- **All UI states coded:** Loading spinner with animated orbit, error with retry, unlock CTA, no-matches empty, populated grid with featured card + match badges. Code is comprehensive.

### ❌ STILL BLOCKING (1 issue):

**All three screenshots show the ATTAIRE landing page — for the seventh consecutive iteration.** The screenshot pipeline (discord-bot.js lines 785-982) is well-written — it sets a mock JWT via `addInitScript`, navigates to `/?tab=twins`, mocks all API responses including `/api/style-twins`, waits for `.style-twin-featured` selector, and has manual fallbacks to click the Twins tab. **The code is correct.** But the app never gets past the onboarding screen in the Playwright context, causing every screenshot to capture the "See it. Scan it. Shop it." landing page.

**Root cause:** The `addInitScript` sets `localStorage.setItem("attair_token", token)`, and `App.jsx` line 2597 initializes screen as `Auth.getToken() ? "app" : "onboarding"`. This *should* work. The likely failure is either: (a) the Vite dev server isn't running at `localhost:5173` when the Playwright script executes, or (b) a Playwright/Chromium version issue where `addInitScript` doesn't set localStorage before React's initial state evaluation.

**To ship:**
- **Debug the screenshot pipeline** by adding a check that the dev server is running before launching Playwright (e.g., `await fetch('http://localhost:5173').catch(() => { throw new Error('Dev server not running') })`). Alternatively, use `page.goto` to a blank page first, set localStorage directly via `page.evaluate`, THEN navigate to `/?tab=twins` — bypassing `addInitScript` timing issues. This is a ~5-line fix to the pipeline.

The feature code itself is production-quality. The **only** remaining blocker is that I need visual proof the Twins UI actually renders — and the pipeline fix to get that proof is straightforward.

This is iteration 8. The previous build did NOT pass quality review. Fix the issues identified by the judge (see below) and bring this to production quality.

INSTRUCTIONS:
1. Read the relevant files first to understand current state.
2. Build the feature to FULL PRODUCTION quality — not a skeleton, not a placeholder. Ship-ready code.
3. Follow existing patterns in the codebase (design tokens in index.css, component styles in App.css).
4. The app should look and feel as polished as TikTok, Depop, Pickle, or Instagram.
5. After building, run the backend tests: cd attair-backend && npm test
6. Fix any test failures before finishing.
7. Commit your changes with: git add -A && git commit -m "feat: [short summary]"

Do NOT create new files unless absolutely necessary. Build within existing files.
Do NOT change function signatures in attair-backend/src/services/products.js.